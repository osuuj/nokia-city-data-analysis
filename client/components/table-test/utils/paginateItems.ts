3. paginateItems.ts
Main Purpose:
Slices the filtered data into pages based on rowsPerPage.

Main Parts:

Implements paginateItems() for pagination logic.
Determines start and end indexes for slicing.
Returns paginated data for rendering.
