/**
 * Project Enums
 *
 * Contains enum definitions for the project feature.
 */

export enum ProjectCategory {
  Web = 'web',
  AI = 'ai',
  Mobile = 'mobile',
  Desktop = 'desktop',
  Other = 'other',
}

export enum ProjectStatus {
  Active = 'active',
  Planning = 'planning',
  Completed = 'completed',
  OnHold = 'on_hold',
}
