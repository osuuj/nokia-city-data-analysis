interface OverviewData {
  id: string;
  name: string;
  value: number;
  timestamp: string;
  // Add other properties as needed
}

const handleDataChange = (data: OverviewData[]) => {
  // ... existing code ...
};
