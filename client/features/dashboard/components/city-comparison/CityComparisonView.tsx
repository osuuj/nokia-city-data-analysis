interface ComparisonData {
  id: string;
  name: string;
  value: number;
  timestamp: string;
  // Add other properties as needed
}

const handleDataChange = (data: ComparisonData[]) => {
  // ... existing code ...
};
