{
  "devDependencies": {
    "husky": "^9.1.7"
  },"scripts": {
  "prepare": "husky init",
  "lint": "eslint --fix .",
  "format": "prettier --write .",
  "lint-staged": "lint-staged"
},
"lint-staged": {
  "*.{js,ts,tsx}": ["eslint --fix", "prettier --write"]
}
}
