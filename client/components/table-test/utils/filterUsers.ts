// 1. filterUsers.ts
// Main Purpose:
// Filters users based on search, worker type, status, and start date.

// Main Parts:

// Implements filterUsers() to apply all filtering conditions.
// Matches text-based filters like memberInfo.name.
// Handles date comparisons for startDate.