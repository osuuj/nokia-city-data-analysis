// TableContext.tsx
// Main Purpose:
// Provides a React Context for table state and actions.

// Main Parts:

// Wraps state management for sorting, filtering, pagination, and selection.
// Exposes a context provider for easy access to table state.
// Allows components to consume the table state without prop drilling.